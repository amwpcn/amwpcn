{
  "name": "@amwpcn/examples",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "rimraf dist",
    "build": "yarn clean && tsc",
    "start": "yarn build && node dist/index"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.5.3"
  }
}
