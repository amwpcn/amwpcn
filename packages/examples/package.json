{
  "name": "@amwpcn/examples",
  "version": "0.0.3",
  "main": "dist/index",
  "private": true,
  "engines": {
    "node": ">=16"
  },
  "scripts": {
    "clean": "rimraf dist",
    "build": "yarn clean && tsc",
    "start": "yarn build && node dist/index"
  },
  "devDependencies": {
    "@types/express": "^4",
    "@types/node": "^20.14.11",
    "rimraf": "^6.0.1",
    "typescript": "^5.5.3"
  },
  "dependencies": {
    "@amwpcn/step": "workspace:^",
    "express": "^4.19.2"
  }
}
